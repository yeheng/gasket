.assets
.env
*.pyc
dist/
build/
*.egg-info/
*.egg
*.pyc
*.pyo
*.pyd
*.pyw
*.pyz
*.pywz
*.pyzz
.venv/
venv/
__pycache__/
poetry.lock
.pytest_cache/
botpy.log

.agent
.claude
.iflow
.opencode
nanobot
# Python lockfiles only. NOT `*.lock`: that glob silently swallowed
# conga/Cargo.lock, which must be committed for reproducible binary builds
# (and which ci.yml keys its cargo cache on via hashFiles).
uv.lock
Pipfile.lock
pdm.lock
.repomix*
repomix*
config.yaml
.fastembed_cache
.worktrees/
conga/target/
web/src-tauri/target/
web/src-tauri/gen/
.zread
.workbuddy
# SDD plan-execution scratch (ledgers, briefs, review packages)
.superpowers/
docs/superpowers
# local git worktrees (using-git-worktrees skill)
.worktrees/
